from setuptools import setup

setup(name='dashapp',
      version='0.1.1',
      description='A package to run a machine learning app with user interface',
      author='Agbleze Linus',
      license='MIT',
      packages=['dashapp'],
      zip_safe=False
      )

